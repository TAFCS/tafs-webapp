import type { Metadata } from "next";
export const metadata: Metadata = { title: "Bulk Delete Students" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
