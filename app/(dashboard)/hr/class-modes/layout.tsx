import type { Metadata } from "next";
export const metadata: Metadata = { title: "Class Attendance Modes" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
